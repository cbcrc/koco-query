{
    "name": "koco-query",
    "version": "0.1.1",
    "authors": [
        "Maxime Séguin"
    ],
    "description": "Url query helper based on jquery-deparam.",
    "main": "src/query.js",
    "homepage": "https://github.com/cbcrc/koco-query",
    "license": "MIT",
    "scripts": {
        "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
    },
    "dependencies": {
        "jquery": "^2.1.3",
        "requirejs": "^2.1.15",
        "jquery-deparam": "^0.4.2"
    }
}
